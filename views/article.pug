extends layout

block content
	h1= article.title
	h5 Article author 
		span.author= author
	p= article.body
	hr
	if user
		if user._id == article.unique
			a.btn.btn-default(href='/articles/edit/'+article._id) Edit
			a.btn.btn-danger.delete-article( data-id=article._id) Delete